/**
 * Placeholder test suite.
 * ------------------------------------------------------------------
 * The scaffold intentionally contains no business logic to test.
 * Real tests will be added alongside each implemented phase
 * (Auth, Resume, Job, Application, Recommendation, ...).
 *
 * Kept here so the test runner has an entry point and CI is wired up.
 */
import { describe, it, expect } from 'vitest';

describe('scaffold', () => {
  it('boots the test runner', () => {
    expect(true).toBe(true);
  });
});
